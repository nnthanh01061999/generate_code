import { Meta } from '@storybook/addon-docs';
import Code from '@/stories/assets/code-brackets.svg';
import Colors from '@/stories/assets/colors.svg';
import Comments from '@/stories/assets/comments.svg';
import Direction from '@/stories/assets/direction.svg';
import Flow from '@/stories/assets/flow.svg';
import Plugin from '@/stories/assets/plugin.svg';
import Repo from '@/stories/assets/repo.svg';
import StackAlt from '@/stories/assets/stackalt.svg';
import Image from 'next/image';

<Meta title="Example/Form" />

<style>
    {`
    .subheading {Ø
      --mediumdark: '#999999';
      font-weight: 900;
      font-size: 13px;
      color: #999;
      letter-spacing: 6px;
      line-height: 24px;
      text-transform: uppercase;
      margin-bottom: 12px;
      margin-top: 40px;
    }

    .link-list {
      display: grid;
      grid-template-columns: 1fr;
      grid-template-rows: 1fr 1fr;
      row-gap: 10px;
    }

    @media (min-width: 620px) {
      .link-list {
        row-gap: 20px;
        column-gap: 20px;
        grid-template-columns: 1fr 1fr;
      }
    }

    @media all and (-ms-high-contrast:none) {
    .link-list {
        display: -ms-grid;
        -ms-grid-columns: 1fr 1fr;
        -ms-grid-rows: 1fr 1fr;
      }
    }

    .link-item {
      display: block;
      padding: 20px 30px 20px 15px;
      border: 1px solid #00000010;
      border-radius: 5px;
      transition: background 150ms ease-out,</code><code> border 150ms ease-out,</code><br/><code> transform 150ms ease-out;
      color: #333333;
      display: flex;
      align-items: flex-start;
    }

    .link-item:hover {
      border-color: #1EA7FD50;
      transform: translate3d(0,</code><br/><code> -3px,</code><br/><code> 0);
      box-shadow: rgba(0,</code><br/><code> 0,</code><code> 0,</code><code> 0.08) 0 3px 10px 0;
    }

    .link-item:active {
      border-color: #1EA7FD;
      transform: translate3d(0,</code><br/><code> 0,</code><code> 0);
    }

    .link-item strong {
      font-weight: 700;
      display: block;
      margin-bottom: 2px;
    }

    .link-item Image {
      height: 40px;
      width: 40px;
      margin-right: 15px;
      flex: none;
    }

    .link-item span {
      font-size: 14px;
      line-height: 20px;
    }

    .tip {
      display: inline-block;
      border-radius: 1em;
      font-size: 11px;
      line-height: 12px;
      font-weight: 700;
      background: #E7FDD8;
      color: #66BF3C;
      padding: 4px 12px;
      margin-right: 10px;
      vertical-align: top;
    }

    .tip-wrapper {
      font-size: 13px;
      line-height: 20px;
      margin-top: 40px;
      margin-bottom: 40px;
    }

    .tip-wrapper code {
      font-size: 12px;
      display: inline-block;
    }
  `}
</style>

# Welcome to Custom Form

This page is about custom form.

<div className="subheading">Dependencies</div>
<p>
    <code>{`"@hookform/error-message": "^2.0.1",
"@hookform/resolvers": "^2.9.7",
"@reduxjs/toolkit": "^1.8.2",
"@vercel/analytics": "^0.1.7",
"antd": "^5.1.2",
"axios": "^1.2.4",
"cookies-next": "^2.1.1",
"dayjs": "^1.11.7",
"lodash": "^4.17.21",
"mongoose": "^6.8.4",
"next": "^13.1.1",
"next-intl": "^2.10.2",
"next-redux-wrapper": "7.0.2",
"next-swagger-doc": "^0.3.6",
"query-string": "^8.1.0",
"react": "18.2.0",
"react-dom": "18.2.0",
"react-hook-form": "^7.34.0",
"react-query": "^3.39.2",
"react-redux": "7.2.4",
"redux": "4.1.1",
"redux-devtools-extension": "^2.13.9",
"sass": "^1.57.1",
"swagger-ui-react": "^4.15.5",
"yup": "^0.32.11"`}</code>
</p>

<div className="subheading">Interface</div>
<p>
    <code>
        {`import { FormItemProps } from 'antd';
import { ReactNode } from 'react';
\ninterface CommonFormProps<T> {
  name: string;
  label: ReactNode;
  showError?: boolean;
  className?: string;
  wrapperProps?: FormItemProps;
  childProps?: T;
  onChangeCallBack?: (value: any, options?: any) => void;
  onBlurCallBack?: () => void;
}`}
    </code>
</p>

<div className="subheading">Example Usage</div>
<p>
    <code>
        {`import { yupResolver } from '@hookform/resolvers/yup';
import { Button, Form } from 'antd';
import { FormProvider, useForm } from 'react-hook-form';
import * as yup from 'yup';
import InputControl from './input/InputControl';
import InputNumberControl from './input/InputNumberControl';
\ninterface ExampleFormValues {
    name: string;
    age: number;
}
\nfunction Example() {
    const schema = yup.object().shape({
        name: yup.string().required(),
        age: yup.number().required(),
    });\n
    const methods = useForm<ExampleFormValues>({
        defaultValues: { name: '', age: 0 },
        resolver: yupResolver(schema),
    });\n
    const { handleSubmit } = methods;\n
    const onSucess = (data: ExampleFormValues) => {
        console.log(data);
    };\n
    return (
        <FormProvider {...methods}>
            <Form layout="vertical" onFinish={handleSubmit(onSucess)}>
                <InputControl name="name" label="Name" wrapperProps={{ required: true }} />
                <InputNumberControl name="age" label="Age" wrapperProps={{ required: true }} />
                <Button htmlType="submit">Submit</Button>
            </Form>
        </FormProvider>
    );
}\n
export default Example;
`}

</code>

</p>

<div className="subheading">References</div>
<div className="link-list">
    <a className="link-item" href="https://ant.design/components/overview/" target="_blank">
        <Image src={Repo} alt="repo" />
        <span>
            <strong>Ant Design</strong>
           Help designers/developers building beautiful products more flexible and working with happiness
        </span>
    </a>
    <a className="link-item" href="https://react-hook-form.com/" target="_blank">
        <Image src={Direction} alt="direction" />
        <span>
            <strong>React-hook-form</strong>
            Simple form validation with React Hook Form.
        </span>
    </a>
    <a className="link-item" href="https://www.npmjs.com/package/yup" target="_blank">
        <Image src={Code} alt="code" />
        <span>
            <strong>Yup</strong>
            JavaScript schema builder for value parsing and validation
        </span>
    </a>

</div>
